# Route UI/UX Conformance

Generated: 2026-07-01T21:54:36.362516+00:00

This table is based on route files, product chrome source, and the approved reference decision.
Screenshot review is still required for visual polish, spacing, and overlap.

| Route | Status | Finding |
| --- | --- | --- |
| `/about` | passes_static_structure | About route remains outside the app shell as a public trust page. |
| `/app/compliance-paths` | passes_static_structure | App route uses the shared product shell with back and breadcrumb controls. |
| `/app/dashboard` | passes_static_structure | App route uses the shared product shell with back and breadcrumb controls. |
| `/app/explore` | passes_static_structure | App route uses the shared product shell with back and breadcrumb controls. |
| `/app/impact` | passes_static_structure | App route uses the shared product shell with back and breadcrumb controls. |
| `/app` | passes_static_structure | App route uses the shared product shell with back and breadcrumb controls. |
| `/app/relationships` | passes_static_structure | App route uses the shared product shell with back and breadcrumb controls. |
| `/app/reliance-policy` | passes_static_structure | App route uses the shared product shell with back and breadcrumb controls. |
| `/app/requirements` | passes_static_structure | App route uses the shared product shell with back and breadcrumb controls. |
| `/app/review` | passes_static_structure | App route uses the shared product shell with back and breadcrumb controls. |
| `/app/review-packets` | passes_static_structure | App route uses the shared product shell with back and breadcrumb controls. |
| `/app/reviewer-operations` | passes_static_structure | App route uses the shared product shell with back and breadcrumb controls. |
| `/app/settings` | passes_static_structure | App route uses the shared product shell with back and breadcrumb controls. |
| `/app/system` | passes_static_structure | App route uses the shared product shell with back and breadcrumb controls. |
| `/app/updates` | passes_static_structure | App route uses the shared product shell with back and breadcrumb controls. |
| `/authorities/[id]` | passes_static_structure | Route has a visible navigation or return path in source. |
| `/dashboard` | needs_return_path_review | Route exists, but source does not show an obvious return path. |
| `/debug/onboarding` | internal_route_static_only | Internal route exists; do not treat as public product conformance evidence. |
| `/debug/personalization` | internal_route_static_only | Internal route exists; do not treat as public product conformance evidence. |
| `/forum/[id]` | needs_return_path_review | Route exists, but source does not show an obvious return path. |
| `/forum` | needs_return_path_review | Route exists, but source does not show an obvious return path. |
| `/internal/heuristics` | internal_route_static_only | Internal route exists; do not treat as public product conformance evidence. |
| `/onboarding` | needs_return_path_review | Route exists, but source does not show an obvious return path. |
| `/` | passes_static_structure | Route exists, but source does not show an obvious return path. |
| `/pricing` | needs_return_path_review | Route exists, but source does not show an obvious return path. |
| `/profiles/[slug]` | needs_return_path_review | Route exists, but source does not show an obvious return path. |
| `/query` | needs_return_path_review | Route exists, but source does not show an obvious return path. |
| `/regulations/[id]` | passes_static_structure | Route has a visible navigation or return path in source. |
| `/regulations` | needs_return_path_review | Route exists, but source does not show an obvious return path. |
| `/settings` | passes_static_structure | Route has a visible navigation or return path in source. |
| `/sign-in` | passes_static_structure | Route has a visible navigation or return path in source. |
| `/trust` | needs_return_path_review | Route exists, but source does not show an obvious return path. |
